# Environment
.env
.env.local
.venv/
venv/
env/
.envrc
env.bak/
venv.bak/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Runtime
logs/
logs_3/
logs_production/
logs_production_v1/
logs_reproducibility/
logs_reproducibility_2/
logs_validation/
outputs/
outputs_3/
outputs_production/
outputs_production_v1/
outputs_reproducibility/
outputs_reproducibility_2/
outputs_validation/
production/
*.log
analysis/

# Slurm / HPC submission scripts (machine-specific paths, not for repo)
*.sbatch
*.slurm
run_pair_*.sbatch
sbatch_*.sh

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
cover/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# OS
.DS_Store
Thumbs.db

# Project-specific
api_key.txt
.claude/settings.local.json
slides/
data/
load_api_keys.sh
doc/*.pdf
doc/
docs/
scripts/
METHODS.md
analyze_pipeline_log.py
examples/
!examples/sp1_nfya_K562/
!examples/sp1_nfya_K562/**
.string_cache/
# Misc
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
cython_debug/
.ruff_cache/
.pypirc
.ropeproject
.pdm-python
.pdm-build/
celerybeat-schedule
celerybeat.pid
*.sage.py
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
db.sqlite3
db.sqlite3-journal

# ── repository cleanup 2026-07 ─────────────────────────────────────
# Large data / archive / generated dirs that should NOT go to GitHub.
# The processed data needed to reproduce the figures ships via Zenodo (data/),
# so data/ stays ignored above. Code that IS tracked: analyses/ and figures/.
# NOTE: a pattern must be alone on its line. '#' only starts a comment at the
# start of a line, so a trailing comment becomes part of the pattern and the
# rule silently matches nothing.
# analysis_old: 2.5G, superseded analyses, archive only
analysis_old/
# notebook: 98M, render modules replaced by figures/; .ipynb + rendered PNGs
notebook/
# webapp: 207M, HF Spaces report browser, deployed separately
webapp/
# paper: manuscript text, not part of the code release
paper/
# .claude: local Claude Code config / memory (machine-specific paths)
.claude/

# reference_sources: other groups' published supplementary tables, kept for
# provenance only; their licences are the original publishers'
reference_sources/

# build artefacts: the Zenodo data package and any other archive built in-tree
*.zip
*.tar
*.tar.gz
*.tgz

# figures/output holds generated PDFs — regenerated by running fig*.py, not committed
figures/output/

# config: keep only config.yaml for now; the other config variants stay local
config/*
!config/config.yaml

# project-specific, not for the public release
CLAUDE.md
METHODS.md
analyze_pipeline_log.py
doc/
!config/requirements_execution.txt
